// npm install react-bootstrap bootstrap --save подключаем в проект /необязательный/ React-компонент стилизации элементов/компонентов от bootstrap
// import 'bootstrap/dist/css/bootstrap.min.css'; импортируем компонент непосредственно в проект, а именно в index.js
// создаем отдельный файл /например: bootstrap-test.js/
// import {Container, Row, Col} from 'react-bootstrap'; импортируем компонент непосредственно в проект, а именно в bootstrap-test.js
// создаем div с перечислением свойств стилизации: